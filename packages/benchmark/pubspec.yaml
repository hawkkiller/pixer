name: benchmarks
description: Benchmarks for the pixer package
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  benchmark_harness: ^2.3.1
  image: ^4.5.4
  pixer:
    path: ../pixer

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
