# Pixer showcase

Minimal Flutter app demonstrating every Pixer image operation on Android, iOS,
Linux, macOS, Windows, and WebAssembly.

```bash
dart ../pixer/tool/build_wasm.dart web/pixer.wasm
flutter run
```
