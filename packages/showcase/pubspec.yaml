name: showcase
description: A minimal cross-platform showcase for Pixer.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.7

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  pixer: ^0.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/example_img.jpg
